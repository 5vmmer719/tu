@startuml
start
:点击新增按钮，填写生产订单信息;
:前端表单验证;

if (验证通过?) then (是)
  :提交至后端;
  :编码唯一性校验;
  if (编码重复?) then (是)
    :返回错误提示;
    stop
  else (否)
    :业务规则校验，插入订单数据;
  endif

  if (订单类型为整车?) then (是)
    :查询车型默认BOM;
    :展开BOM，计算物料需求;
    :批量生成订单BOM项与子订单;
  else (否)
  endif

  :返回成功响应，刷新列表;
else (否)
  :显示错误提示;
  stop
endif

stop

@enduml