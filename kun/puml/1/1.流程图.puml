@startuml

start
:管理员进入物料管理页面;
:点击新增按钮，填写物料信息;

if (前端表单验证通过?) then (是)
  :提交至后端;
else (否)
  :显示错误提示;
  stop
endif

if (权限校验通过?) then (是)
  :设置审计字段，执行数据库插入;
else (否)
  :返回权限错误;
  stop
endif

if (插入成功?) then (是)
  :关闭对话框，刷新列表;
else (否)
  :显示操作失败提示;
endif

stop

@enduml