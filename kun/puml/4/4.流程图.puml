@startuml

start
:仓管员点击新增调拨单;
:选择调出/调入仓库和货位;

if (源仓库 == 目标仓库?) then (是)
  :提示仓库不能相同;
  stop
else (否)
endif

:填写调拨原因，添加物料明细;

if (前端表单验证通过?) then (是)
  :提交至后端;
else (否)
  :显示错误提示;
  stop
endif

if (权限校验通过?) then (是)
  :生成调拨单号，设置状态和审计字段;
  :批量插入调拨明细;
  :插入调拨单主表记录;
else (否)
  :返回权限错误;
  stop
endif

if (插入成功?) then (是)
  :关闭对话框，刷新列表;
else (否)
  :显示操作失败提示;
endif

stop

@enduml